/**
 * Enum para os tipos de lista que o componente ListaPresenca pode gerenciar.
 * Ajuda a desacoplar a lógica do componente dos nomes específicos de cada contexto.
 */
export const ListaTipo = Object.freeze({
    PRINCIPAL: 'principal',
    SECUNDARIA: 'secundaria',
});