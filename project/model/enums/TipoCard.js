/**
 * Enum for tipoCard attribute values - for generating the CardActions according to the context.
 * @readonly
 * @enum {string}
 */
export const TipoCard = Object.freeze({
    COMPOSICAO_URNA: 'composicao-urna',
    CONSELHO_SENTENCA: 'conselho-sentenca'
});